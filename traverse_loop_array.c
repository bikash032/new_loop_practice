#include<stdio.h>

int main(){
    // define the array 
 int arr[]={1,2,3,4,5};
   // define the number of array
   int n = sizeof(arr)/sizeof(arr[0] );
   for (int i =0;i<1;i++)
   {
    printf("%d\n",arr[3]);
   }
return 0;
}